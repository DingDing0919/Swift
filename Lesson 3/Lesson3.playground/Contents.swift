// Tuple

import Foundation

    //import Darwin
    //let (x, y) = (1, 2)
    //x
    //y

//
// =, +, -, *, /, %
// +=, -=, *=, /=, %=
    //var a  = 10
    //a     += 10

// Bool
// Binary Operator
// ==, >=, <=, != , >, <
    //1 == 1
    //2 != 1
    //3 <= 2
    //4 >= 5
    //5 != 5


// Ternary Operator
// Bool ? (True / False) : (True / False)
    //let ContentHeight = 40
    //let HasHeader     = true
    //let RowHeight     = ContentHeight + (HasHeader ? 50 : 30)

// Nil(Null) Operator
// Type <-(OutPut) Optional Type ?? Type
// (IntPut) -> Optional Type
// (OutPut) -> Type

// e.g.
    //let defaultColorName     = "red"
    //var userDefinedColoeName : String?
    //let ColorNameToUse       = userDefinedColoeName ?? defaultColorName
    //
    //ColorNameToUse


// Range Operator
// ...
// ..<
    //for i in 1...5{
    //    print(i)
    //}

let names = ["Go", "Do", "Fo", "Ho"]
for i in names{
    print(i)
}
